1) configure the inventories/dev/hosts, list your VMs for the master and workers

2) configure your ssh public key and copy the file in files/id_rsa.pub
  - you can set up your ssh key using the ssh-keygen command with all default values

3) configure the ansible admin user, use your default admin user
ansible-playbook utils/add_ansible_user.yml -u jfranco -b -k

4) run the main playbook
ansible-playbook main.yml
